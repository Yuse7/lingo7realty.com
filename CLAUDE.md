# Lingo7 Realty — контекст проекта

> Этот файл Claude Code читает автоматически в начале каждой сессии. Здесь — всё, что нужно, чтобы продолжить работу без исходного чата.

## Что это

**Lingo7 Realty** — лендинг (статический сайт) для подготовки к экзамену на **лицензию риелтора в США** (US real estate license), нацеленный на людей, **для которых английский не родной**. Ключевая идея: взять модель exam-prep (как у PrepAgent) и «прошить» в неё языковой инструментарий приложения **Lingo7** (параллельный перевод, тап-сохранение слов, озвучка, интервальное повторение). Так не-носитель закрывает сразу два барьера — предмет и язык, — чего у обычных prep-сервисов нет.

## Происхождение и связанные проекты

- **Форк от Lingo7** — родительский проект-лендинг приложения для изучения языков: `/Users/sergey/PC/Projects/L7` (домен lingo7.ru). Отсюда взяты вся вёрстка, `styles.css`, ассеты и интерактивное демо. Lingo7 Realty — **суб-бренд** L7.
- **Модель — PrepAgent** (real-estate exam prep, принадлежит Aceable, Inc.; AceableAgent — их же бренд пре-лицензионных курсов). Скриншоты внутреннего кабинета PrepAgent лежат в `./inside/` (27 шт., визуальный референс функционала: флешкарты «термин→определение», пробные тесты multiple-choice с разбором «Here's why», глоссарий с поиском, аудио-уроки, e-book, видео-уроки с расшифровкой, вебинары, тьюторинг, crash-курсы, рефералка). `inside/` в `.gitignore` — это референс, не часть сайта.

## Ключевые решения (зафиксированы с пользователем 2026-06-03)

- **Базовый язык — русский.** Первая версия как локализуемый шаблон-источник; мультиязычность (исп., и т.д.) — позже. В демо термины экзамена на английском → перевод на русский.
- **Бренд — «Lingo7 Realty»**, суб-бренд L7: тот же логотип/зелёная палитра, латинское имя + зелёный акцент «Realty» (класс `.logo__accent`).
- **Отдельный проект-сосед** (эта папка), оригинал L7 не трогаем.

## Стек и архитектура

- Чистый статический сайт: **HTML + CSS + ванильный JS**, без сборщиков и фреймворков. Превью = просто открыть `index.html`.
- `index.html` — весь лендинг (14 секций) + 6 блоков `<script>` в конце (модалка, sticky-CTA, бургер, орбитальный метод, ревилы/табы/чипы/аккордеон, интерактивное демо).
- `styles.css` — **унаследован от L7 без изменений**, плюс в конце дописан блок `===== Lingo7 Realty — exam-prep additions =====` с новыми классами (см. ниже). При правках стилей: общие компоненты уже есть в L7-части, не дублируй — переиспользуй.
- Секции (в порядке): hero · trust-strip · compare · метод (orbital) · 4 шага · «Что внутри» (library-табы) · «Загрузи свой курс» (custom-book) · для кого (audience) · боли→решения (pains) · штаты (бывш. languages) · отзывы · FAQ · финальный CTA · footer.
- Служебные страницы: `about/contacts/privacy/terms.html`. SEO: `sitemap.xml`, `robots.txt`, домен-плейсхолдер `lingo7realty.com`.

### Новые CSS-классы (мокапы продукта, только в этом проекте)
`.examhome` (экран-каталог в hero и step 01), `.flashcard` / `.flashcard--lg` (карточка термина с двуязычным определением), `.qcard` (вопрос теста + переведённый разбор), `.gloss` (двуязычный глоссарий), `.wordcard-mock` (карточка слова, step 03), `.srs-mock` (интервальное повторение, step 04), `.reader__lang` (чип языка в демо), `.header__cta`, `.logo__accent`, `.mock-frame`.

### Интерактивное демо в hero (НЕ сломать)
Внутри телефона в hero — самодостаточный IIFE (последний `<script>`, «Interactive exam-card demo»). Цикл на `async/await`:
1. Экран-каталог `#readerCatalog` (`.examhome`) → палец тапает «Начать обучение» (`#studyStart`).
2. Двуязычная «читалка» (`.reader__pair` = англ. строка `.reader__en[data-line]` + рус. `.reader__ru`): караоке-озвучка по словам, затем палец тапает термин (`lien` в строке 0, `Escrow` в строке 2) → попап → «Перевод» (карточка `#readerCard`) → «Добавить» → тост + новая карточка в деку (`#vocabNew`, `#vocabNew2`).
Данные терминов — объекты `lienData`/`escrowData` в коде. Палец позиционируется через `getBoundingClientRect` (нужен реальный браузер; в headless-скрине анимации не видно).

## Статус

**Готово:** полный лендинг на русском, все секции переписаны под продукт, интерактивное демо, CSS-мокапы функционала, чистка от L7 (убраны Яндекс.Метрика, CNAME lingo7.ru, план L7).

**Плейсхолдеры — заменить перед запуском:**
1. **Отзывы** в секции reviews — выдуманные цитаты «первых тестеров».
2. **Юр-страницы** (about/contacts/privacy/terms) — текст ещё от L7 (про изучение языков), сделан только бренд/домен-свап. Нужен переписанный под exam-prep.
3. **Ссылки на сторы** (App Store / Google Play) ведут на приложение L7.
4. **Домен** `lingo7realty.com` — заглушка (canonical/OG/sitemap/robots). При деплое подставить реальный + создать `CNAME`.

## Бэклог / следующие шаги

- Переписать юр-страницы под продукт.
- Заменить отзывы на реальные/нейтральные заготовки.
- Вторая локаль (испанская — крупнейший сегмент не-носителей в недвижимости США) поверх этого шаблона.
- Углубить демо: добавить живой сценарий «вопрос теста» с тапом по ответу и переведённым разбором (сейчас он статичным мокапом в секции «Что внутри»).
- Настоящие скриншоты приложения (сейчас в library/steps — CSS-мокапы).

## Превью / проверка вёрстки

```bash
open index.html
# Скриншот через headless Chrome:
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless --disable-gpu \
  --hide-scrollbars --window-size=1400,1050 --screenshot=/tmp/shot.png "file://$PWD/index.html"
# Вся страница в PDF (hero растягивается на 100vh, поэтому tall-screenshot не годится):
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless --disable-gpu \
  --no-pdf-header-footer --print-to-pdf=/tmp/page.pdf "file://$PWD/index.html"
```
NB: секции `compare` и `audience` появляются по scroll-reveal (IntersectionObserver) — в статичном PDF/скрине они пустые, в живом браузере проявляются при прокрутке. Это норма, унаследовано от L7.

## Справка: связь PrepAgent / Aceable (из ресёрча)
PrepAgent и AceableAgent — **бренды одной компании Aceable, Inc.** (Остин, Техас). Aceable купила PrepAgent ~2 июня 2021. AceableAgent = обязательные пре-лицензионные курсы; PrepAgent = подготовка к экзамену (всё на английском). Наша ниша/преимущество — языковая поддержка для не-носителей, которой у них нет.
